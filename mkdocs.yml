site_name: FennFlow
site_description: GenAI Agent Framework, the Pydantic way
strict: true
theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          paths: [ src ]

nav:
  - Home: index.md
  - API: api.md